{
  "name": "opencode-gaia-sandbox",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "remoteUser": "node",
  "workspaceFolder": "/workspaces/opencode-gaia",
  "forwardPorts": [4096],
  "portsAttributes": {
    "4096": {
      "label": "OpenCode Web",
      "onAutoForward": "notify"
    }
  },
  "postCreateCommand": "bun --version && opencode --version && cd tools/opencode-gaia-plugin && bun install && bun run check && cd ../opencode-gaia-harness && bun install && bun run check && bun run cli bootstrap",
  "postStartCommand": "bun run --cwd tools/opencode-gaia-harness cli bootstrap",
  "customizations": {
    "vscode": {
      "extensions": [
        "oven.bun-vscode"
      ]
    }
  }
}
